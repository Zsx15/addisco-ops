services:
  app:
    build: .
    ports:
      - "8501:8501"
    env_file: .env
    environment:
      - DB_PATH=/app/data/database.db
    volumes:
      - addisco_data:/app/data
    restart: unless-stopped

volumes:
  addisco_data:
